[project]
name = "uci-mtb-results"
version = "0.1.0"
description = "A python app built for scraping mountain bike race results from the UCI MTB website."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "duckdb>=1.1.3",
    "requests>=2.32.3",
    "selenium>=4.27.1",
]
